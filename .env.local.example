# Copia este archivo como .env.local y llena los valores.
# En Vercel: Project Settings → Environment Variables.

# Supabase → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://wuuxsezlplwrxlpaiwub.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOi...   # anon public key (Supabase → API → Project API keys → anon public)

# Solo servidor (NO lleva NEXT_PUBLIC). Supabase → API → service_role key.
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOi...        # ⚠ secreta: nunca la subas al repo ni la compartas
